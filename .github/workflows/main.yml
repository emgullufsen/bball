name: bball-CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      
      # this is not part of deploy
      # - name: decrypt apper ssh key
      #   run: ./.github/scripts/dk.sh
      #   env: 
      #     DKPASS: ${{ secrets.DKPASS }}

      - name: Get Dependencies
        run: |
          echo running lein deps...
          lein deps

      # - name: Run Tests
      #   run: |
      #     echo running lein test...
      #     lein test :not-db

      - name: Build App
        run: |
          echo running lein uberjar...
          lein uberjar

      - name: Parting Message
        run: |
          echo Alrighty
          echo Should be good to go...
          
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/actions/ansible
        env:
          DKPASS: ${{ secrets.DKPASS }}
